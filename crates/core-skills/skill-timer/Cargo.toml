[package]
name = "skill-timer"
version = "0.1.0"
edition = "2021"
description = "Timer skill: create macOS Clock timers with optional name"

[dependencies]
async-trait = "0.1"
thiserror = "1.0"
metrics = "0.21"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
