[package]
name = "skill-shopping-list"
version = "0.1.0"
edition = "2021"
description = "Shopping list skill: add/remove items in Apple Notes"

[dependencies]
async-trait = "0.1"
thiserror = "1.0"
metrics = "0.21"
chrono = { version = "0.4", features = ["clock"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
