[package]
name = "skill-distance"
version = "0.1.0"
edition = "2021"
description = "Distance skill: distance between two places (origin/destination; current location as default)"

[dependencies]
async-trait = "0.1"
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
skill-weather = { path = "../skill-weather" }
thiserror = "1.0"
