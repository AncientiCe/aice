[package]
name = "skill-weather"
version = "0.1.0"
edition = "2021"
description = "Weather skill: Open-Meteo integration"

[dependencies]
async-trait = "0.1"
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1.0"
