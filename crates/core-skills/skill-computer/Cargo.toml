[package]
name = "skill-computer"
version = "0.1.0"
edition = "2021"
description = "Computer-use skill: browser, apps, file automation"

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
thiserror = "1.0"
metrics = "0.21"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
