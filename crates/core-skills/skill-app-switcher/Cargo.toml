[package]
name = "skill-app-switcher"
version = "0.1.0"
edition = "2021"
description = "App switcher skill: control macOS app focus and lifecycle"

[dependencies]
async-trait = "0.1"
thiserror = "1.0"
metrics = "0.21"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
