[package]
name = "core-runtime-protocol"
version = "0.1.0"
edition = "2021"
description = "Protocol types for aice backend/frontend runtime split"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
