[package]
name = "core-search"
version = "0.1.0"
edition = "2021"
description = "Fallback web search with user confirmation"

[dependencies]
async-trait = "0.1"
thiserror = "1.0"
reqwest = { version = "0.12", features = ["json"] }
url = "2"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
